package ${basePackageName}.${projectName}.${moduleName}.mapper;

import ${basePackageName}.${projectName}.${moduleName}.domain.${className};
import ${basePackageName}.core.framework.base.BaseMapper;

/**
 * ${tableComment} Mapper接口
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Mapper extends BaseMapper<Long, ${className}> {
}
